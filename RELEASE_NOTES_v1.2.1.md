# Cymrise Color Picker v1.2.1

## 轻量原生版

- 原生 Win32 C++ 实现。
- 发行压缩包只包含 `ColorPicker.exe`。
- 不包含 .NET Runtime、PDB、`.config` 或外部 DLL。
- 用户解压后直接运行，无需安装应用运行时。

## 稳定性与取色准确性

- Per-Monitor V2 DPI 感知。
- 使用完整虚拟桌面物理坐标采样。
- 支持多显示器、不同缩放比例、负坐标布局与键盘确认。
- 取消取色不会限制鼠标。
