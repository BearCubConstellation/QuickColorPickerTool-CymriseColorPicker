# Cymrise Color Picker v1.2.0

## 原生轻量版

- 完全改为原生 Win32 C++ 实现。
- 发布物为单个 `ColorPicker.exe`。
- 无需安装 .NET、Java 或额外应用运行时。
- 使用静态 C/C++ 运行库，发布包不包含 PDB、配置文件或运行时目录。

## 取色修复

- 启用 Per-Monitor V2 DPI 感知。
- 使用虚拟桌面物理像素坐标采样，修复 125% / 150% 等缩放偏移。
- 支持多显示器、负坐标布局与键盘确认。
- 移除鼠标裁剪逻辑，取消取色不会锁定鼠标。
